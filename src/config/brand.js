// ---------------------------------------------------------
// Edit this file to re-brand the whole site for a different
// client sample — name, tagline, and logo mark all come from
// here. Nothing else should hardcode "Pulse" as a literal string.
// ---------------------------------------------------------
import { LineChart } from 'lucide-react'

export const BRAND = {
  name: 'XM MARKETS',
  tagline: 'Portfolio management built for account managers, earn massively here.',
  LogoIcon: LineChart
}
